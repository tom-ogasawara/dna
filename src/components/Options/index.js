import React, { Component } from 'react';
import './style.css';

class Options extends Component {
  render() {
    return (
      <div>
        options
      </div>
    );
  }
}

export default Options;